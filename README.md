# cs5001-github-repo-agent
cs5001 assignment to create a github repo agent

# Run the CLI
Right now, the current method of running the CLI is: 

`agent [review or draft] [OPTION]`

For example:
`agent review --base main`
`agent draft pr --base main`